<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="Board" >
	<select id="getBoardList" parameterType="_int" resultType="fulldto">
		SELECT * FROM BOARD WHERE movie_id=#{movie_id}
	</select>
	<insert id="addComment" parameterType="boarddto">
		INSERT INTO BOARD
		VALUES (board_num_seq.nextval, #{board_content}, #{board_date}, 0, 'Y', #{user_num}, #{movie_id})
	</insert>
	
	<select id="getCommentCnt" parameterType="_int" resultType="_int">
		SELECT count(*) FROM "COMMENT" WHERE BOARD_NUM = #{board_num}
	</select>
	

</mapper>